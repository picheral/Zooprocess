Dialog.create("FIRST OBJECT");
Dialog.addMessage(" ZOOM - MOVE to the object to split   \n \n Press Roll Mouse Button before drawing line \n OK to draw NEW line \n CANCEL to END drawing lines");
Dialog.show();
val = 1;
ret = toString(val);
return ret;
